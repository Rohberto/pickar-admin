import SignUpComponent from "@/features/auth/sign-up";

export const metadata = {
  title: "Create Account",
};

const CreateAccountPage = () => {
  return <SignUpComponent />;
};

export default CreateAccountPage;
